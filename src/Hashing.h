//
// Created by cchau on 12/26/2024.
//

#ifndef PLAYGROUND_HASHING_H
#define PLAYGROUND_HASHING_H

#include <string>
#include <vector>

class Hashing {
public:
    bool checkIfPangram(const std::string& sentence);
    int missingNumber(const std::vector<int>& nums);
    int countElements(const std::vector<int>& arr);
    int largestUniqueNumber(const std::vector<int>& nums);
    int maxNumberOfBalloons(const std::string& text);
    int findMaxLength(const std::vector<int>& nums);
};


#endif //PLAYGROUND_HASHING_H
